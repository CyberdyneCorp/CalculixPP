# Phase 3 — Thermal & Contact

Implements baseline thermal/contact/model-change physics plus the one new capability
(`contact-search`). Each task cites the capability it satisfies. Depends on
`phase-1-foundation` and `phase-2-nonlinear-statics-and-materials`.

## 1. Contact-search engine (spec: contact-search [NEW])

- [ ] 1.1 Spatial proximity search (bounding structure) with configurable search/adjust distance
- [ ] 1.2 Node-to-surface projection: closest point, signed gap, local normal/tangent frame
- [ ] 1.3 Surface-to-surface mortar segment integration (dual + standard Lagrange basis)
- [ ] 1.4 Gap / pressure-overclosure evaluation and tangential stick/slip state
- [ ] 1.5 Contact operator generation assembled via ComputeBackend/NumPP within the Newton iteration
- [ ] 1.6 Expose gap/pressure/area state for thermal contact reuse

## 2. Contact modeling (spec: contact)

- [ ] 2.1 `*CONTACT PAIR` (node-to-surface + surface-to-surface) and `*SURFACE INTERACTION`
- [ ] 2.2 `*SURFACE BEHAVIOR` (hard / exponential / linear / tied pressure-overclosure)
- [ ] 2.3 `*FRICTION` (Coulomb, stick stiffness, stick↔slip)
- [ ] 2.4 Thermal contact: `*GAP CONDUCTANCE`, `*GAP HEAT GENERATION`
- [ ] 2.5 Contact modifiers + output (`*CLEARANCE`, `*CONTACT FILE`/`PRINT`/`OUTPUT`, CSTR)

## 3. Heat transfer (spec: heat-transfer-analysis)

- [x] 3.1 Steady-state conduction element kernels; `*HEAT TRANSFER, STEADY STATE` — scalar 1-DOF/node temperature field parallel to the mechanical path: `fem::element_conduction` (Kt_e = Σ_gp (g_a·g_b) k detJ w, reusing the shared shape functions/Gauss/`physical_gradients`), `fem::assemble_conduction` (thermal DOF numbering + Dirichlet elimination of prescribed temperatures), `*CFLUX`/`*DFLUX` into the thermal rhs (`fem::thermal_load_vector`; DFLUX reuses a generalized face integrator now covering tet/hex/wedge faces), and `numerics::solve_heat_transfer` (SPD Kt T = q via `solve_reduced`). CLI + Python auto-dispatch on `*HEAT TRANSFER`. VALIDATED against stock CalculiX `oneel20cf` (*CFLUX, NT abs err ~1.8e-15, RFL ~3.6e-14) and `oneel20df` (*DFLUX S1 hex-face, NT ~1.3e-15) plus an analytical 1-D bar (linear profile + Fourier Q=kAΔT/L). Transient (3.2)/film (3.3)/radiation (3.4) remain deferred.
- [x] 3.2 Transient conduction (backward Euler) — capacitance `C_e = ∫ rho*c N_a N_b dV` (`fem::element_capacitance`, `Model::element_heat_capacity` from `*SPECIFIC HEAT` × `*DENSITY`) assembled on the full node numbering (`fem::assemble_full_thermal`) and time-stepped by `numerics::solve_transient`: backward Euler `(C/dt + Kt_film) T_{n+1} = (C/dt) T_n + q`, marching the step period (`increment.total`) in `increment.initial` sub-steps. Parser now accepts a transient `*HEAT TRANSFER` (no STEADY STATE) and `*INITIAL CONDITIONS, TYPE=TEMPERATURE`; CLI + Python auto-dispatch. VALIDATED analytically: (a) a unit cube convecting on all six faces keeps the field spatially UNIFORM, so the FEM reduces EXACTLY to the lumped ODE `Cv dT/dt = -h A (T-T_sink)` — the driver reproduces the backward-Euler recurrence to ~1e-8 (accounting for the default linear ramp of the unamplified sink); (b) a long transient relaxes to the independently-computed steady conduction+film field (~1e-3). (tests/test_thermal_transient.cpp)
- [x] 3.3 Convective film loads (`*FILM`) — `q_film = h (T - T_sink)` on a face adds `h ∫N_a N_b dA` to `Kt` and `h T_sink ∫N_a dA` to the rhs, via the shared face surface integrator `fem::face_surface_integrals` (reuses the `*DFLUX`/pressure face quadrature, all tet/hex/wedge faces). Wired into both the steady (`assemble_conduction` / `assemble_full_thermal`) and transient paths; parser handles `*FILM elem,F<face>,sink,h`. VALIDATED against stock CalculiX `oneel20fi` (single C3D20R cube, `*FILM 1,F1,-12,10`): nodal temperatures (NT −2/−1/0 profile) and heat generation (RFL, conduction-only) match to ~1e-14 (Python regression corpus). Forced-convection NETWORK film coupling is out of scope (Phase-5 1-D networks) — solid-only surface film done.
- [x] 3.4 Radiation (`*RADIATE`) — SURFACE-TO-AMBIENT `q = eps sigma (T^4 - T_amb^4)` implemented and linearized (Newton on `dq/dT = eps sigma 4 T_abs^3`) inside the thermal iteration, in absolute temperature via `*PHYSICAL CONSTANTS` (ABSOLUTE ZERO / STEFAN BOLTZMANN); works in both steady and transient. Parser handles `*RADIATE elem,R<face>,ambient,eps`. VALIDATED analytically against a conduction↔radiation equilibrium (`k(T_hot−T)/L = eps sigma (T_abs^4−T_amb_abs^4)`, scalar-root reference, ~1e-3). CAVITY radiation (`*RADIATE ...,CR`, gray-body surface-to-surface exchange) is now IMPLEMENTED: each `R<face>CR` face is a diffuse-gray enclosure patch (`Radiate::cavity`, parser strips the `CR` suffix). `fem::build_cavity` (src/fem/cavity_radiation.cpp) gathers each patch (centroid/area/outward-normal + shared face integrals, normal oriented out of the solid via the element centroid) and computes the view-factor matrix `F_ij = (1/A_i) ∫_Ai ∫_Aj cos_i cos_j/(pi r^2) dA_j dA_i` by direct double-area quadrature over a REFINED (sub=6) face sampling (`fem::face_gauss_points(...,sub)`, reusing the `*FILM`/`*DFLUX` face machinery), then row-normalizes to the summation rule `Σ_j F_ij = 1` (closed enclosure). `fem::cavity_heat_flow` solves the gray-body radiosity system `(I-(1-eps)F) J = eps sigma T^4` (dense Gauss-Jordan; black-body `eps=1` handled directly) and returns the per-patch net heat loss `Q_i` PLUS its exact temperature tangent `dQ_i/dT_k`; the driver (`numerics::add_cavity_radiation`) distributes `Q_i/A_i` over the patch nodes via the face load shape and assembles the full patch-coupled tangent into the same Newton system as surface-to-ambient radiation (steady + transient share the one nonlinear loop; the cavity geometry is built once per step). VALIDATED ANALYTICALLY (tests/test_cavity_radiation.cpp): (a) the summation rule `Σ_j F_ij = 1` and, for two facing patches, `F12=F21=1`; (b) the raw double-area kernel reproduces the KNOWN cube view factors (hollow-cube cavity: opposite face F≈0.1998, adjacent F≈0.2001, Hottel) within ~2e-2 on the coarse quadrature; (c) two facing patches (a 2-body enclosure) give the classic gray-body parallel-plate net flux `q = sigma (T1^4−T2^4)/(1/e1+1/e2−1)` to ~1e-9 across several emissivity pairs incl. the black-body limit, with the analytic tangent finite-difference-checked; PLUS a Python end-to-end deck (python test_cavity_radiation_parallel_plates_python) where two thin high-k plates held at 800/500 K exchange only by cavity radiation and the hot-plate flux reaction equals the parallel-plate `q` (≈13120 W) with energy conserved. No clean in-scope stock-CalculiX cavity `.dat.ref` is matched: the stock decks (beamhtcr/axrad) rely on CalculiX's hemicube+shadowing view-factor engine, out of scope for the direct-quadrature method — the analytical parallel-plate/cube references validate the physics instead. The `*VIEWFACTOR` read/write-to-file control (a solver-workflow directive, not the physics) remains rejected with an actionable message. (spec: heat-transfer-analysis — radiation with view factors.)

## 4. Coupled thermomechanics (spec: heat-transfer-analysis — coupled)

- [x] 4.1 `*COUPLED TEMPERATURE-DISPLACEMENT` — MONOLITHIC 4-DOF/node (u,v,w,T) coupling implemented and validated (and the one-way path retained as the decoupled limit). The parser recognizes `*COUPLED TEMPERATURE-DISPLACEMENT[, SOLUTIONS=MONOLITHIC|STAGGERED]` (`Procedure::Coupled` + `Model::coupled_scheme`), routing the thermal cards (temperature BCs on DOF 11, `*CFLUX`/`*DFLUX`/`*FILM`) to the scalar field and the mechanical loads to the displacement field. `numerics::solve_coupled` dispatches on the scheme. The MONOLITHIC path (`numerics::solve_monolithic_once`, src/numerics/heat_transfer.cpp) assembles ONE combined reduced system that stacks the mechanical free block `[0,nf_m)` and the thermal free block `[nf_m,nf_m+nf_t)` — `[[K_uu, K_uT],[0, K_TT]] [u;T] = [f_ext; q]` — reusing the already-reduced mechanical (`assemble_linear_static`, with its constraint transform) and thermal (`assemble_conduction`, with its Dirichlet map) operators, and the NEW off-diagonal `K_uT` thermal-strain coupling block `C_e[a][j] = ∫ B_aᵀ D {alpha,alpha,alpha,0,0,0} N_j dV` (`fem::element_thermal_coupling`, the exact derivative of the thermal-strain load w.r.t. nodal temperature), reduced through the mechanical transform (rows) and the thermal Dirichlet map (cols) with prescribed temperatures / the `-Tref` constant moved to the rhs, then solved once via `solve_reduced`. The mechanical->thermal block `K_Tu` is the plastic-dissipation heat source (see below); with no two-way term the 4-DOF system is block-triangular, so the monolithic solve reproduces the sequential thermal-then-mechanical result EXACTLY. CLI + Python (`solve`/`solve_text`) auto-dispatch on the procedure and the result dict carries both temperature and displacement/stress/reaction. VALIDATED: (a) analytical thermal stress end to end — a bar with a prescribed linear temperature profile and u_x fixed on both end faces develops sigma_xx = -E alpha T_bar (tests/test_thermal_stress.cpp test_coupled_deck_end_to_end + python test_coupled_temperature_displacement_thermal_stress); (b) MONOLITHIC == STAGGERED (== one-way) for the thermal-stress deck to solver tolerance (test_monolithic_equals_oneway + python test_coupled_monolithic_equals_staggered, temperature ~1e-9, stress ~1e-6); (c) the genuine two-way term — plastic-dissipation (Taylor-Quinney) heating — makes both schemes agree on the joint (T,u) state (test_two_way_plastic_dissipation).
- [x] 4.2 Iterated (Gauss-Seidel) STAGGERED coupling — implemented with an outer convergence check (`numerics::solve_coupled_staggered`). It alternates a thermal solve (`solve_heat_transfer`) and a mechanical solve (`solve_linear_static`, or `solve_nonlinear_static` for a plastic model), feeding the mechanical plastic-dissipation heat back into the next thermal solve, and exits when the temperature field stops changing (`< 1e-9`). The mechanical->thermal two-way term is plastic-dissipation heating: a Taylor-Quinney fraction `Model::taylor_quinney` of the per-element plastic work density `sigma_y(ep) ep V_e` (`Model::plastic_dissipation_heat`, from the per-element committed eqplastic that `solve_nonlinear_static` now reports via its new `eqplastic_by_elem` out-parameter) is deposited as a lumped nodal heat source (`*CFLUX`-equivalent, energy-conserving). CONVERGES IN ONE PASS when the coupling is one-way (`taylor_quinney==0` or no plasticity) — byte-for-byte the historical sequential result — and ITERATES when the two-way term is active. VALIDATED: test_two_way_plastic_dissipation (a stretched-past-yield element deposits dissipation heat; the staggered outer count > 1 and the temperature rises above the conducted baseline, while monolithic converges to the same state) + test_monolithic_equals_oneway (outer count == 1 for the one-way case). `numerics::solve_coupled_reported` exposes the outer-iteration count.
- [x] 4.3 Thermal expansion coupling verified against reference thermal-stress decks — `*EXPANSION[, ZERO=Tref]` parsed into `Material::expansion` (isotropic alpha + reference/stress-free temperature Tref) and consumed by the mechanical path via `Model::element_expansion()`. The equivalent thermal-strain load f_th = ∫ Bᵀ D (alpha (T - Tref)) dV (`fem::element_thermal_load` / `fem::thermal_strain_load_vector`, reusing the shared shape functions / Gauss / physical_gradients) is added to the mechanical rhs, and stress recovery uses sigma = D (eps_mech - eps_th), so a temperature field induces the correct thermal stress/strain while the internal force f_int = Ke u - f_th keeps the reactions RF consistent. Gated on `Model::has_thermal_strain()` so a deck with no applied temperature is byte-for-byte the pure-mechanical path (all Phase-1/2 reference matches unchanged). VALIDATED (a) ANALYTICALLY: a fully axially-constrained bar heated by dT develops sigma_xx = -E alpha dT (and sigma_yy=sigma_zz=0), and a free bar expands by alpha dT L with zero stress (tests/test_thermal_stress.cpp); (b) against a REFERENCE DECK: stock CalculiX `beamt` (C3D20R "heated beam between two fixed walls", `*EXPANSION,ZERO=293` + `*TEMPERATURE 500`) reproduces the `.dat.ref` nodal displacements to rel-L2 ~2.4e-6 (python REFERENCE_CORPUS). Temperature-dependent `alpha(T)` tables are now consumed (evaluated at the element mean temperature) — see task 6.1.

## 5. Model change (spec: model-change)

- [x] 5.1 `*MODEL CHANGE, TYPE=ELEMENT` deactivate / reactivate element sets (strain-free reactivation), WITHIN-STEP **and** CROSS-STEP — DONE via the new MULTI-STEP ENGINE. A per-element active mask (`Model::deactivated_elements` / `Model::element_active_mask()`, aligned with `mesh.elements()`) is consulted by every assembled system: `REMOVE`d elements contribute no stiffness/internal force (`assemble_linear_static`, `assemble_material_tangent`), no stress/reaction (`stress.cpp` recover_fields + internal_force), no body/pressure/thermal-strain load (`loads.cpp`), and no conduction/capacitance (`thermal.cpp`). `TYPE=ELEMENT, ADD` for an id drops it from the deactivated set (reactivation). The parser accepts `*MODEL CHANGE, TYPE=ELEMENT, ADD|REMOVE` with element ids or elset names on the data lines; the deactivated set ACCUMULATES across steps (REMOVE adds, ADD erases) and is snapshotted per step. The mask is all-true when no element is removed, so a deck without `*MODEL CHANGE` is byte-for-byte the pre-model-change mechanical path. MULTI-STEP ANALYSIS (the enabler): `io::parse_inp_steps` splits a deck into one `Model` per `*STEP...*END STEP` block (loads/BCs/procedure/controls/active-mask carried forward, OP=NEW resetting per step, `*CHANGE SOLID SECTION`/`*CHANGE MATERIAL` rebinding at the boundary; a single-`*STEP` deck returns exactly one model equal to `parse_inp()`'s). `numerics::solve_multistep_static` (src/numerics/multistep.cpp) solves each step INCREMENTALLY from the previous converged displacement: `K(A_k) du = f_ext(k) - K(A_k) u_prev` with prescribed increments, accumulating displacement/stress/strain/internal-force. A constant active set reproduces the single-step total-load solve EXACTLY (linear superposition); a reactivated element enters `K_ff` only in the steps it is active, so it is STRAIN-FREE relative to the DEFORMED configuration at reactivation. `*BOUNDARY, FIXED` (new `Spc::fixed`) holds a DOF at its carried deformed value (prescribed increment zero). The single-step path is guarded (`steps.size()==1` -> `solve_linear_static`), so it is byte-identical (all Phase-1/2/3 reference matches + the nonlinear-reproduces-linear gate unchanged). VALIDATED: (a) ANALYTICALLY (tests/test_multistep.cpp + python test_multistep_two_load_steps_superpose / test_multistep_single_step_equals_solve): the single-step driver equals solve_linear_static to ~1e-14; two linear load steps of F sum to the single-step 2F result (displacement/reaction/stress) to solver tolerance; cross-step birth-death (two stacked confined cubes, cube 2 removed in step 1 then re-added in step 2) shows the re-added element carrying stress built ONLY from the step-2 increment — its increment matches the never-removed reference's step-2 increment while its total is smaller (it skipped step-1's stress), and a stay-removed element is exactly zero (python test_multistep_element_birth_death_python); (b) against a stock-CalculiX MULTI-STEP REFERENCE DECK — `beampfix` (C3D20R cantilever, step 1 `*CLOAD`, step 2 `*CLOAD, OP=NEW` + new load + `*BOUNDARY, FIXED` holding the LOAD set at its deformed value): BOTH steps' `*NODE PRINT` displacements reproduced to rel-L2 ~1e-7 across all 261 nodes (python test_multistep_beampfix_matches_calculix), incl. node 117 uy=0.4710545 matching the `.dat.ref` exactly. The `*CHANGE SOLID SECTION` cross-step deck `changesolidsection` (E halved in step 2) doubles the displacement exactly (ratio 2.000). CLI + Python (`solve_multistep`/`solve_multistep_text`, list of per-step dicts) auto-run multi-step decks; the CLI writes the final step's `.frd`/`.dat`.
- [~] 5.2 `*MODEL CHANGE, TYPE=CONTACT PAIR` activate / deactivate contact pairs between steps — PARSE + STORE only (contact assembly is the separate contact workflow). The parser accepts `*MODEL CHANGE, TYPE=CONTACT PAIR, ADD|REMOVE` with a `surface_a, surface_b` data line and records each as a `ContactPairChange{surface_a, surface_b, add}` on `Model::contact_pair_changes`. No contact search/force/constraint is assembled from it yet — the record is stored for the contact workflow (task 2.x) that will consult it. VALIDATED: tests/test_model_change.cpp test_contact_pair_stored (record content + ADD/REMOVE sense) and python test_model_change_contact_pair_parses_python (the deck solves through Python, the pair does not perturb the mechanical solve). Between-step activation semantics need the multi-step engine (deferred with 5.1).

## 6. Materials, results, parser, bindings, validation

- [x] 6.1 Consume `*CONDUCTIVITY` / `*SPECIFIC HEAT` in thermal kernels, INCLUDING temperature-dependent tables (spec: material-models) — `*CONDUCTIVITY` / `*SPECIFIC HEAT` / `*EXPANSION` are parsed into piecewise-linear `PropertyTable`s of (value, temperature) rows (`Material::Thermal` / `Expansion`), each row ending in a temperature; `PropertyTable::at(T)` interpolates linearly and CLAMPS outside the table. A SINGLE-row (constant) table is byte-for-byte the pre-table path (validated: `test_constant_table_unchanged`, and all steady/transient reference matches unchanged). Temperature-dependent conductivity/capacitance are evaluated at each element's MEAN nodal temperature (`Model::element_conductivity_at()` / `element_heat_capacity_at()`), and the steady/transient drivers Picard-iterate the assembly (`assemble_full_thermal(model, lambda, node_temp)`) to convergence when `Model::has_temp_dependent_thermal()` (radiation shares the same iteration). Temperature-dependent `alpha(T)` is evaluated at the element mean applied temperature in the thermal-strain load and stress recovery (`fem/loads.cpp`, `fem/stress.cpp`). VALIDATED ANALYTICALLY against the Kirchhoff transform: a 1-D bar with `k(T)=50+0.5T`, ends 0/100, has constant flux `q L = ∫_0^100 k dT = 7500`, giving the mid-plane temperature `Tm=-100+√25000≈58.11` (NOT the constant-k value 50) and hot-face heat rate `q A = 3750` — reproduced by the FEM to ~1e-2 (`tests/test_thermal.cpp test_temp_dependent_conductivity_bar` + python `test_temperature_dependent_conductivity_python`); temperature-dependent `alpha(T)` verified via `sigma_xx=-E alpha(T_bar) dT` (`test_constrained_bar_temp_dependent_alpha`).
- [x] 6.2 Thermal result fields `NT` / `HFL` in `.frd` / `.dat` (spec: results-output), INCLUDING integration-point HFL — `NT` (nodal temperature) written to `.frd` (NDTEMP) and `.dat`, and the concentrated heat-flux reaction `RFL` to `.dat`. Integration-point heat flux `HFL` is now recovered as `q = -k(T) grad(T)` at each Gauss point (`fem::recover_heat_flux`), extrapolated to nodes via the SHARED stress-extrapolation scheme (`fem/extrapolation.hpp` — C3D4 constant / C3D10 a4+midside / hex-wedge Gauss-mean, refactored out of `stress.cpp` so stress and flux use the identical code), emitted to `.frd` (FLUX vector dataset, nodal) and to `.dat` as the `*EL PRINT HFL` block `heat flux (elem, integ.pnt.,qx,qy,qz)` at the integration points (CalculiX's convention). The Python thermal result dict carries `heat_flux` (nodal Nx3), `hfl_elem`/`hfl_gp`/`hfl_flux` (integration-point). VALIDATED against the exact Fourier flux `q = k dT/L` on the 1-D bar (`tests/test_thermal.cpp test_heat_flux_bar`, qx=-5000 at every GP + node) AND against the stock-CalculiX REFERENCE DECK `oneel20cf.dat.ref` `*EL PRINT HFL` block (C3D20R cube, uniform qx=120, 8 integration points matched to ~1e-13 — python `test_heat_flux_hfl_matches_calculix`).
- [~] 6.3 Extend deck parser to the Phase-3 card set — THERMAL card set parses gracefully end to end (`*HEAT TRANSFER[,STEADY STATE]`, `*CONDUCTIVITY`, `*SPECIFIC HEAT`, `*DENSITY`, `*EXPANSION[,ZERO=]`, `*COUPLED TEMPERATURE-DISPLACEMENT`, `*PHYSICAL CONSTANTS`, `*INITIAL CONDITIONS,TYPE=TEMPERATURE`, `*CFLUX`, `*DFLUX S<face>`, `*FILM elem,F<face>,sink,h`, `*RADIATE elem,R<face>,amb,eps`, `*RADIATE ...,CR` (gray-body cavity, task 3.4), `*TEMPERATURE`, `*MODEL CHANGE`); a bare `*` separator line (seen in stock decks) is now skipped so `oneel20fi2` parses. Every Phase-3 CONTACT card (`*CONTACT PAIR`, `*SURFACE INTERACTION`, `*SURFACE BEHAVIOR`, `*FRICTION`, `*GAP CONDUCTANCE`, `*GAP HEAT GENERATION`, `*CLEARANCE`) and the still-deferred thermal controls (`*VIEWFACTOR` view-factor file read/write directive, `*DFLUX ...,BF` body flux) raise a clear, actionable ParseError naming the deferral/next-workflow — never a crash or silent wrong solve (src/io/inp_parser.cpp reject_card). Surface-name-based `*DFLUX SUR,S` and network-film (`THERMAL NETWORK`) decks remain out of scope for this slice; contact-card parsing stays [~] until the contact workflow.
- [~] 6.4 Extend Python bindings + API-parity coverage test — the thermal solve surface is fully reachable from Python: `solve`/`solve_text` auto-dispatch `*HEAT TRANSFER` (steady/transient) and `*COUPLED TEMPERATURE-DISPLACEMENT` and return `temperature` (NT) + `flux_reaction` (RFL) in the result dict; `summary`/`summary_text` report the thermal `procedure` without solving. New parity test `test_api_parity_phase3_thermal` asserts the thermal solve fields, the summary procedure discrimination (heat transfer vs static), and that a Phase-3 CONTACT card is rejected with an actionable RuntimeError. No NEW binding entry point was needed (the thermal path rides `solve`/`summary`), so this extends coverage rather than the API surface — [~] until the contact bindings land alongside the contact workflow.
- [~] 6.5 Phase-3 reference corpus (steady/transient conduction, film, radiation, n2f + mortar contact, friction, coupled temp-disp, model change) + per-deck tolerances — THERMAL corpus curated with per-deck tolerances in python/tests/test_regression.py (THERMAL_CORPUS: oneel20cf/df/fi steady @1e-9, oneel20fi2 transient @1e-4; REFERENCE_CORPUS: beamt coupled thermal stress @1e-4) plus analytical thermal tests (coupled temp-disp, radiation/transient in the C++ suite). Radiation and coupled use analytical references where no clean in-scope .dat.ref deck exists (stated in the manifest comments). CONTACT/friction/mortar decks remain for the contact workflow — [~].
- [x] 6.6 pytest regression: each Phase-3 thermal deck matches reference CalculiX within tolerance — `test_thermal_reference_corpus_matches_calculix` is parametrized over THERMAL_CORPUS (steady mode compares the single .dat block, transient mode marches backward-Euler and compares the FINAL increment) and skips gracefully when a reference .inp/.dat.ref is absent; `test_reference_corpus_matches_calculix[beamt]` covers the coupled thermal-stress deck. All pass against the stock CalculiX test tree. (Contact-deck regression is the contact workflow.)
- [x] 6.7 Update README/docs with a thermal example; Phase-3 status reflects the thermal track — README gains a "Python — heat transfer (Phase 3)" example (unit-cube conduction returning NT/RFL, transient/coupled notes), a Phase-3 THERMAL status blockquote (steady/transient/film/radiate/coupled/model-change + "contact is next"), a thermal accuracy bullet in Validation, and roadmap/timeline status "thermal landed; contact next". All mermaid diagrams keep ascii labels and stay valid. (Contact example deferred with the contact workflow.)
